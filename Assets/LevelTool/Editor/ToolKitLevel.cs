using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class ToolKitLevel : EditorWindow
{
/*    public GUIStyle headerStyle;
    [MenuItem("Window/Level Tool")]
    public static void ShowWindow()
    {
        GetWindow<ToolKitLevel>("Level Tool");
    }
    public void OnEnable()
    {
        //InitializeStyles();
        Init();
    }
    private void DrawBody()
    {
        EditorGUILayout.BeginHorizontal();
        ViewLevelTool.OnDrawLevelNav();
        ViewWaveTool.OnDrawWaveNav();
        ViewSpawnerTool.OnDrawSpawnerNav();
        EditorGUILayout.EndHorizontal();
    }
    private void OnGUI()
    {
        DrawBody();
        // DrawHeader();
    }

    private void Init()
    {
        ViewLevelTool.InitializeStyles();
        ViewWaveTool.InitializeStyles();
        ViewSpawnerTool.InitializeStyles();
    }
    private void DrawHeader()
    {
        EditorGUILayout.BeginHorizontal();
        GUILayout.Label("Level Editor", headerStyle, GUILayout.Height(position.height * 0.2f), GUILayout.Width(position.width));
        EditorGUILayout.EndHorizontal();
    }
    private void InitializeStyles()
    {
        headerStyle = GUIViewCotroller.CreateStyle(20, FontStyle.Bold, Color.magenta, TextAnchor.MiddleCenter);
    }
*/
   
}
